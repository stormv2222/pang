# Phase 1 설계 - 메인 화면

## 목표

브라우저에서 게임을 실행하면 메인 화면이 표시되고,  
게임 시작 / 게임 종료 버튼이 동작한다.

---

## 화면 구성 (레이아웃)

```
┌─────────────────────────────┐
│                             │
│                             │
│           PANG              │  ← 게임 타이틀
│                             │
│        [ 게임 시작 ]         │  ← 시작 버튼
│        [ 게임 종료 ]         │  ← 종료 버튼
│                             │
│                             │
└─────────────────────────────┘
```

- 전체 화면을 채우는 고정 배경
- 타이틀과 버튼은 수직 중앙 정렬
- 고정 캔버스 사이즈: **640 × 480px**

---

## 화면 전환 구조

화면은 `App.tsx`에서 `currentScreen` 상태로 관리한다.  
Phase 1에서는 `'main'` 상태만 구현하며, `'game'`은 빈 화면으로 대체한다.

```
'main'  ──[게임 시작]──▶  'game'  (Phase 2에서 구현)
  ▲                           │
  └───────────────────────────┘ (Phase 13에서 연결)
```

---

## 컴포넌트 구조

```
App.tsx
  └── MainScreen.tsx       ← 메인 화면 전체
        ├── 타이틀 텍스트
        ├── 시작 버튼
        └── 종료 버튼
```

### App.tsx 역할
- `currentScreen` 상태 보유 (`'main' | 'game'`)
- 상태에 따라 `<MainScreen />` 또는 빈 게임 화면 렌더링

### MainScreen.tsx 역할
- 타이틀, 버튼 렌더링
- 버튼 클릭 이벤트를 props로 상위(`App.tsx`)에 전달

---

## 파일 구조

```
src/
  App.tsx
  screens/
    MainScreen.tsx
```

---

## 버튼 동작

| 버튼 | 동작 |
|------|------|
| 게임 시작 | `currentScreen`을 `'game'`으로 변경 → 빈 게임 화면으로 전환 |
| 게임 종료 | 브라우저 confirm 다이얼로그 표시 → 확인 시 탭 닫기 시도 (`window.close()`) |

> `window.close()`는 브라우저 보안 정책상 직접 열린 탭에서는 동작하지 않을 수 있다.  
> 이 경우 "브라우저 탭을 직접 닫아주세요" 안내 문구를 표시한다.

---

## 스타일 방향

- 별도 CSS 프레임워크 없이 인라인 스타일 또는 CSS 모듈 사용
- 배경색: 어두운 단색 (예: `#1a1a2e`)
- 타이틀: 크고 굵은 흰색 텍스트
- 버튼: 테두리 스타일, 호버 시 색상 변경

---

## 검토 요청 사항

- 게임 종료 버튼의 동작 방식이 적절한가? (`window.close()` vs 안내 문구)
- 고정 캔버스 사이즈 **640 × 480px**이 적절한가?
- 화면 전환을 `currentScreen` 상태로 관리하는 방식이 괜찮은가?
